local icons = require("icons")

M42 = {
  icons = icons,
  colorscheme = 'gruvbox',
    ui = {
    float = {
      border = 'rounded',
      highlight = 'gruvbox' -- check available by :Telescope highlights
    }
  },
}
